---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import Background from '../components/Background.astro';
import ProjectCard from '../components/ProjectCard.astro';
import { SITE_TITLE, SITE_DESCRIPTION, AUTHOR, TAGLINE, SKILLS, SOCIAL } from '../consts';
import { getCollection } from 'astro:content';

const allProjects = (await getCollection('projects')).sort(
	(a, b) => b.data.date.valueOf() - a.data.date.valueOf(),
);
const featured = allProjects.filter((p) => p.data.featured).slice(0, 3);
const highlight = featured.length ? featured : allProjects.slice(0, 3);
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<style>
			main {
				width: 960px;
				max-width: calc(100% - 2em);
			}
			.hero {
				padding: 3em 0 1.5em;
				display: grid;
				grid-template-columns: 1.05fr 1fr;
				align-items: center;
				gap: 2.5em;
			}
			.hero-visual {
				padding: 0;
				overflow: hidden;
				line-height: 0;
				border: 1px solid var(--border);
				border-radius: var(--radius-lg);
				box-shadow: var(--box-shadow);
				transition: transform 0.3s cubic-bezier(0.16, 1, 0.3, 1), border-color 0.3s ease, box-shadow 0.3s ease;
			}
			.hero-visual:hover {
				transform: translateY(-4px);
				border-color: var(--border-hover);
				box-shadow: 
					0 25px 50px rgba(0, 0, 0, 0.3),
					0 0 20px rgba(var(--accent-rgb), 0.15);
			}
			.hero-visual img {
				width: 100%;
				height: auto;
				display: block;
				border-radius: inherit;
			}
			@media (max-width: 820px) {
				.hero {
					grid-template-columns: 1fr;
					gap: 1.8em;
				}
				.hero-visual {
					transform: none !important;
					box-shadow: var(--box-shadow) !important;
					order: -1;
				}
			}
			.badge {
				display: inline-flex;
				align-items: center;
				gap: 0.5em;
				padding: 0.4em 1em;
				margin-bottom: 1.5em;
				font-size: 0.8rem;
				font-weight: 600;
				color: rgb(var(--gray-dark));
				background: rgba(var(--accent-rgb), 0.08);
				border: 1px solid rgba(var(--accent-rgb), 0.2);
				border-radius: 999px;
			}
			.badge .dot {
				width: 8px;
				height: 8px;
				border-radius: 50%;
				background: #22c55e;
				box-shadow: 0 0 0 0 rgba(34, 197, 94, 0.5);
				animation: pulse 2s infinite;
			}
			@keyframes pulse {
				0% {
					box-shadow: 0 0 0 0 rgba(34, 197, 94, 0.5);
				}
				70% {
					box-shadow: 0 0 0 6px rgba(34, 197, 94, 0);
				}
				100% {
					box-shadow: 0 0 0 0 rgba(34, 197, 94, 0);
				}
			}
			.hero h1 {
				font-size: clamp(2.5rem, 6vw, 3.75rem);
				margin: 0 0 0.3em;
				line-height: 1.05;
				font-family: "Outfit", sans-serif;
				font-weight: 700;
			}
			.hero h1 .grad {
				background: linear-gradient(135deg, var(--accent-dark), var(--accent-cyan));
				-webkit-background-clip: text;
				background-clip: text;
				-webkit-text-fill-color: transparent;
			}
			.hero p {
				font-size: 1.25rem;
				color: rgb(var(--gray));
				margin: 0 0 1.5em;
				max-width: 38ch;
			}
			.cta {
				display: flex;
				gap: 1em;
				flex-wrap: wrap;
			}
			.cta a {
				display: inline-block;
				padding: 0.8em 1.8em;
				border-radius: var(--radius);
				text-decoration: none;
				font-weight: 600;
				font-size: 0.95rem;
				transition: transform 0.2s cubic-bezier(0.16, 1, 0.3, 1), box-shadow 0.2s cubic-bezier(0.16, 1, 0.3, 1), background-color 0.2s ease;
			}
			.cta a.primary {
				background: var(--accent);
				color: var(--on-accent);
				box-shadow: 0 8px 24px rgba(var(--accent-rgb), 0.35);
			}
			.cta a.primary:hover {
				transform: translateY(-2px);
				background-color: var(--accent-dark);
				box-shadow: 0 12px 32px rgba(var(--accent-rgb), 0.45);
			}
			.cta a.secondary {
				border: 1px solid var(--border);
				color: rgb(var(--black));
				background: rgba(var(--surface-rgb), 0.45);
				backdrop-filter: blur(10px);
				-webkit-backdrop-filter: blur(10px);
			}
			.cta a.secondary:hover {
				transform: translateY(-2px);
				border-color: var(--border-hover);
				background: rgba(var(--surface-rgb), 0.75);
			}
			section {
				margin: 3.5em 0;
			}
			h2 {
				font-size: 1.6rem;
			}
			.skills {
				display: flex;
				flex-direction: column;
				gap: 1.2em;
			}
			.skill-group {
				display: flex;
				flex-wrap: wrap;
				align-items: baseline;
				gap: 0.5em;
			}
			.skill-group .group-label {
				font-weight: 600;
				min-width: 220px;
				color: rgb(var(--black));
				font-size: 0.9rem;
			}
			.skill-group ul {
				display: flex;
				flex-wrap: wrap;
				gap: 0.5em;
				list-style: none;
				margin: 0;
				padding: 0;
			}
			.skill-group li {
				padding: 0.3em 0.8em;
				font-size: 0.82rem;
				background: rgba(var(--surface-rgb), 0.45);
				border: 1px solid var(--border);
				border-radius: 999px;
				color: rgb(var(--gray-dark));
				transition: border-color 0.2s ease, color 0.2s ease, background-color 0.2s ease;
			}
			.skill-group li:hover {
				border-color: var(--border-hover);
				color: rgb(var(--black));
				background: rgba(var(--accent-rgb), 0.1);
			}
			.section-head {
				display: flex;
				justify-content: space-between;
				align-items: baseline;
				margin-bottom: 1.2em;
			}
			.section-head h2 {
				margin: 0;
			}
			.section-head a {
				font-size: 0.95rem;
				text-decoration: none;
				font-weight: 600;
				color: var(--accent-dark);
				transition: color 0.2s ease;
			}
			:root[data-theme="light"] .section-head a {
				color: var(--accent);
			}
			.section-head a:hover {
				opacity: 0.85;
			}
			.grid {
				display: grid;
				grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
				gap: 1.5em;
			}
			@media (max-width: 720px) {
				.skill-group .group-label {
					min-width: 100%;
					margin-bottom: 0.2em;
				}
			}
		</style>
	</head>
	<body>
		<Background />
		<Header />
		<main>
			<section class="hero reveal">
				<div class="hero-copy">
					<span class="badge"><span class="dot"></span>Available for projects</span>
					<h1>Hi, I'm <span class="grad">{AUTHOR}</span></h1>
					<p>{TAGLINE}</p>
					<div class="cta">
						<a class="primary" href="/projects">View projects</a>
						<a class="secondary" href={SOCIAL.github} target="_blank" rel="noopener">GitHub</a>
						<a class="secondary" href={`mailto:${SOCIAL.email}`}>Contact</a>
					</div>
				</div>
				<div class="hero-visual glass-strong">
					<img src="/hero-terminal.svg" alt="Minimalist hacker-style terminal running a DevOps deploy session" width="760" height="560" loading="eager" />
				</div>
			</section>

			<section>
				<h2>Tech stack</h2>
				<div class="skills">
					{
						SKILLS.map((group) => (
							<div class="skill-group">
								<span class="group-label">{group.group}</span>
								<ul>
									{group.items.map((item) => <li>{item}</li>)}
								</ul>
							</div>
						))
					}
				</div>
			</section>

			{
				highlight.length > 0 && (
					<section>
						<div class="section-head">
							<h2>Featured projects</h2>
							<a href="/projects">View all →</a>
						</div>
						<div class="grid">
							{highlight.map((project) => <ProjectCard project={project} />)}
						</div>
					</section>
				)
			}
		</main>
		<Footer />
	</body>
</html>
